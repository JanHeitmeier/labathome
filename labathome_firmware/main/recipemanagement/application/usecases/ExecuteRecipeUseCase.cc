#include "ExecuteRecipeUseCase.hh"

// Implementierung für ExecuteRecipeUseCase.
ExecuteRecipeUseCase::ExecuteRecipeUseCase(/* ... */) {
    // ...
}

void ExecuteRecipeUseCase::execute(const std::string& recipeName) {
    // 1. Rezept aus IRecipeStorage laden.
    // 2. RecipeEngine anweisen, das Rezept auszuführen.
    // 3. Ggf. Live-View über den Status informieren.
}
