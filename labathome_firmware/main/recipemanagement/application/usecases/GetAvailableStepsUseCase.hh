#pragma once

#include <vector>

// Use Case zum Abrufen aller verfügbaren Step-Typen.
// Holt die Metadaten aus der StepTypeRegistry und konvertiert sie in ein DTO.
class GetAvailableStepsUseCase {
public:
    GetAvailableStepsUseCase(/* Abhängigkeiten wie StepTypeRegistry hier injecten */);
    // AvailableStepsDto execute();
};
