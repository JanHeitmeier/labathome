#pragma once

#include <string>

// Use Case zum Erstellen eines neuen Rezepts.
class CreateRecipeUseCase {
public:
    CreateRecipeUseCase(/* ... */);
    // bool execute(const RecipeDto& recipeData);
};
